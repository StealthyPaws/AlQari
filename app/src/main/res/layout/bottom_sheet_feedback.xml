<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/bottomSheetContainer"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="24dp"
    android:background="@drawable/bg_duo_card">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="16dp">

        <TextView
            android:id="@+id/feedbackTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Excellent!"
            android:textSize="24sp"
            android:textStyle="bold"
            android:fontFamily="@font/nunito"
            android:textColor="@color/duo_light_green" />

        <ImageView
            android:id="@+id/feedbackIcon"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:src="@drawable/ic_check_circle"
            app:tint="@color/duo_light_green"
            android:visibility="gone"/> 
            <!-- Icon is optional, keeping it simple for now as per request -->
    </LinearLayout>

    <TextView
        android:id="@+id/feedbackMessage"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Perfect articulation."
        android:textSize="18sp"
        android:fontFamily="@font/nunito"
        android:textColor="@color/duo_text_primary"
        android:layout_marginBottom="32dp" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnGotIt"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:text="GOT IT"
        android:textSize="18sp"
        android:textStyle="bold"
        android:fontFamily="@font/nunito"
        android:textColor="@color/white"
        app:cornerRadius="16dp"
        android:backgroundTint="@color/duo_light_green"
        android:stateListAnimator="@null"
        android:elevation="0dp" />

</LinearLayout>
